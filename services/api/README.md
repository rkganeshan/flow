# flow-api

HTTP API for Flow.

## Env

- `DATABASE_URL` (required)
- `PORT` (default: 3000)
- `CORS_ORIGIN` (optional, comma-separated)

## Endpoints (early MVP)

- `GET /healthz`
- `GET /readyz`
- `GET /v1/tenants`
- `POST /v1/tenants`
